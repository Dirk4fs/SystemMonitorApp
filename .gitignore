# Navigate to project
cd /c/Projects/SystemMonitorApp

# Stage the updated .gitignore
git add .gitignore

# Commit the change
git commit -m "Improve .gitignore with VS and build exclusions"

# Push to GitHub
git push origin main
